{
  "name": "easeinall",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "web": "yarn workspace @easeinall/translation_helper dev",
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test"
  }
}
